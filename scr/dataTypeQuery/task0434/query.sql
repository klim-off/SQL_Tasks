/*
 Write a query that will select the length of employee names in bytes and characters from the employee table.
 */
-- Write your code here:

SELECT LENGTH(name), CHARACTER_LENGTH(name) FROM employee